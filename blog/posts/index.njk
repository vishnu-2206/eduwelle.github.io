---
layout: layout.njk
title: EduWelle Blog
---

<h1>EduWelle Blog</h1>
<p class="center-text">Insights, ideas, and updates from the EduWelle Labs team.</p>

<div class="blog-grid">
  {% for post in collections.all | reverse %}
    {% if "posts/" in post.inputPath and post.data.title != "EduWelle Blog" %}
      <div class="blog-card">
        {% if post.data.cover %}
          <img src="{{ post.data.cover }}" alt="Cover image" class="cover" />
        {% endif %}
        <div class="card-content">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p class="date">{{ post.date | readableDate }}</p>
          {% if post.templateContent %}
            <p>{{ post.templateContent | striptags | truncate(160, true, '...') }}</p>
          {% endif %}
          <a href="{{ post.url }}" class="read-more">Read More →</a>
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>
