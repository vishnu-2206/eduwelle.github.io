---
title: EduWelle Blog
layout: layout.njk
---

<h1>EduWelle Blog</h1>
<p style="text-align:center;">Insights, ideas, and updates from the EduWelle Labs team.</p>

<div class="blog-grid">
  {% for post in collections.all | reverse %}
    {% if post.data.title and post.inputPath.includes("/posts/") and post.data.title != "EduWelle Blog" %}
      <div class="blog-card">
        {% if post.data.cover %}
          <img src="{{ post.data.cover }}" alt="{{ post.data.title }}" class="cover">
        {% endif %}
        <div class="card-content">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p class="date">{{ post.date | readableDate }}</p>
          {% if post.data.excerpt %}
            <p>{{ post.data.excerpt }}</p>
          {% endif %}
          <a href="{{ post.url }}" class="read-more">Read More →</a>
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>

<p class="back-link"><a href="/">← Back to EduWelle Home</a></p>
